/*import React from 'react';
import expect from 'expect';
import { shallow, mount, render } from 'enzyme';
import Shop from './shop';*/

/*xdescribe('A suite', () => {
  xit('contains spec with an expectation', () =>
    expect(shallow(<Shop><div /></Shop>).contains(<div className="main" />)).toBe(true),
  );

  xit('contains spec with an expectation', () =>
    expect(shallow(<Shop><div /></Shop>).is('.main')).toBe(true),
  );

  xit('contains spec with an expectation', () =>
    expect(mount(<Shop><div /></Shop>).find('.main').length).toBe(1),
  );

  xit('can run an expectation with render', () =>
    expect(render(<Shop><div /></Shop>).find('.main').length).toBe(1),
  );
});*/
